import {
  hashColor
} from '../../../helpers/hash-color';
import { module, test } from 'qunit';

module('HashColorHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hashColor(42);
  assert.ok(result);
});
