{{#if showPageHeader}}
  {{#if isOutput}}
    [/indent]
    	[b](Showing page {{currentPage}} of {{endPage}})[/b]
    [indent]
  {{else}}
    <b>(Showing page {{currentPage}} of {{endPage}})</b>
  {{/if}}
{{/if}}

{{yield}}
